fun sleep(secs) {
  var start = clock();
  var diff = 0;

  while (diff < 1) {
    diff = clock() - start;
  }

  return "tihi";
}

print "sleeping for 1 sec...";
print sleep(1);
print "done sleeping";
